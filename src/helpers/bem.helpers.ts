import { setup } from 'bem-cn';

export const block = setup({
  el: '__',
  mod: '--',
});

export const absoluteBlock = block('Absolute');
export const circleBlock = block('Circle');
export const smallCircleBlock = block('SmallCircle');
export const menuItemBlock = block('MenuItem');
export const splitterBlock = block('Splitter');
